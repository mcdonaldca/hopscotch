<h1>users</hi>

<h2> List of People </h2>
<ul>
<% @users.each do |user| %>
<li>
<%= user.first %> (<%= link_to "View", user %>, <%= link_to "Edit", edit_user_url(user) %>, 
  <%= link_to "Delete", user_url(user), method: 'delete' %>)
</li>
<% end %>
</ul>
<%= link_to "Add a New user", new_user_url %>

	